spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Joe192168/TestConfig.git
          default-label: master
          search-paths: testInfo