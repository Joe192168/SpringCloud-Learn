server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Joe192168/SpringCloud-Learn.git
          searchPaths: testInfo
        default-label: master
  application:
    name: config-server